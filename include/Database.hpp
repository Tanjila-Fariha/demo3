#ifndef DATABASE_HPP
#define DATABASE_HPP
#include <vector>
#include <string>

class InventoryDB
{
public:
    // This will test if the AST parser handles 'for' loops correctly
    bool checkStock(std::string itemID);
};
#endif